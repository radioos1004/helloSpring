
# deploy.sh 사용방법
### Execute Shell Command
cd demo <br>
chmod +x deploy.sh  # 실행 권한 부여 확인 <br>
./deploy.sh         # sudo 없이 실행 <br>



# deploy_v1.sh 사용법
### 2. 사용 방법

스크립트에 실행 권한을 준 뒤, 원하는 옵션을 붙여 실행합니다.

#### ① Java (Gradle) 배포 시
```bash
cd demo <br>
chmod +x deploy.sh  # 실행 권한 부여 확인 <br>
./deploy.sh -n my-java-app -t java-gradle -p 8081 -d ./my-project
```

#### ② .NET Core 배포 시
```bash
cd demo <br>
chmod +x deploy.sh  # 실행 권한 부여 확인 <br>
./deploy.sh -n my-dotnet-app -t dotnet -p 5000 -d ./dotnet-project
```

---

### 3. 개선된 점 및 참고사항

1.  **매개변수화 (`getopts`)**: `-n`, `-t`, `-p` 등의 옵션을 통해 스크립트 수정 없이 실행 시점에 값을 변경할 수 있습니다.
2.  **프로젝트 타입별 분기 (`case`)**:
    *   `java-gradle`: `./gradlew` 사용
    *   `java-maven`: `mvn` 사용 (필요 시)
    *   `dotnet`: `dotnet publish` 사용
3.  **유연한 포트 설정**: `-p` 옵션으로 외부 노출 포트를 지정할 수 있습니다. 내부 포트는 기술 스택의 관례에 따라 자동으로 설정되도록 로직을 넣었습니다 (.NET은 보통 80, Spring은 8080).
4.  **이미지 및 컨테이너 이름 자동화**: 앱 이름을 기반으로 컨테이너 이름이 중복되지 않게 설정됩니다.
5.  **Dockerfile 작성 팁**:
    *   이 스크립트는 빌드를 **호스트 OS**에서 수행하고 결과물(jar, dll)만 Docker로 넘기는 방식입니다.
    *   따라서 `Dockerfile` 작성 시 `COPY build/libs/*.jar app.jar` (Java) 또는 `COPY ./publish .` (.NET) 처럼 이미 빌드된 파일을 복사하는 형태여야 속도가 빠릅니다.

### 4. 더 나은 방법: 환경 설정 파일 (.env)
만약 프로젝트마다 매번 긴 명령어를 치기 귀찮다면, 각 프로젝트 폴더에 `.env` 파일을 만들고 스크립트에서 이를 읽어오게 할 수도 있습니다.

**스크립트 상단에 추가:**
```bash
if [ -f .env ]; then
  export $(echo $(cat .env | sed 's/#.*//g' | xargs) | envsubst)
fi
```
이렇게 하면 프로젝트별로 다른 설정값들을 파일로 관리할 수 있어 관리가 훨씬 편해집니다. (Jenkins나 GitHub Actions 같은 CI 도구를 쓴다면 위 스크립트 방식이 가장 표준적입니다.)

